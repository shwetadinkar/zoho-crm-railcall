# Regenerated by `railcall market module sign` on every edit.
# Only meaningful against one exact tree, so committing it creates
# noise in every diff and a stale signature in every clone.
module.sig

__pycache__/
*.pyc

# Local probe output and captured org data - never publish.
backup_*.json
merge_experiment_state.json
merge_experiment.py
apply_061.py
